import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# LoadDirectory
<Method
	name="PIXEL.LoadDirectory"
	realm="shared"
	args={[
		<Argument type="string" name="path">The path to the directory to load from.</Argument>
	]}
	returns={[
		<ReturnValue type="table" name="files">The names of every file in the folder.</ReturnValue>,
		<ReturnValue type="table" name="folders">The names of every folder in the folder.</ReturnValue>,
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/autorun/sh_pixelui_loader.lua#L22-L43"
>
	Includes every Lua file from the given folder.
</Method>