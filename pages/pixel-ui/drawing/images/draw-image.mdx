import { Callout } from "nextra/components";

import Method from "/components/method";
import Argument from "/components/method/argument";

# DrawImage
<Method
	name="PIXEL.DrawImage"
	realm="client"
	args={[
		<Argument type="number" name="x">The X coordinate to draw at.</Argument>,
		<Argument type="number" name="y">The Y coordinate to draw at.</Argument>,
		<Argument type="number" name="width">The width of the image.</Argument>,
		<Argument type="number" name="height">The height of the image.</Argument>,
		<Argument type="string" name="url">The URL of the image.</Argument>,
		<Argument type="Color" name="color">The color to tint the image.</Argument>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/drawing/cl_images.lua#L47-L65"
>
	Draws an image downloaded from the given URL.
	Images are automatically downloaded if they are not present in the cache.
	A progress wheel will be shown in place while the image is being downloaded.
</Method>