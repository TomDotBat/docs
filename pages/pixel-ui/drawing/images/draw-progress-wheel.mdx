import { Callout } from "nextra/components";

import Method from "/components/method";
import Argument from "/components/method/argument";

# DrawProgressWheel
<Method
	name="PIXEL.DrawProgressWheel"
	realm="client"
	args={[
		<Argument type="number" name="x">The X coordinate to draw at.</Argument>,
		<Argument type="number" name="y">The Y coordinate to draw at.</Argument>,
		<Argument type="number" name="width">The width of the area to draw within.</Argument>,
		<Argument type="number" name="height">The height of the area to draw within.</Argument>,
		<Argument type="Color" name="color">The color to tint the progress wheel.</Argument>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/drawing/cl_images.lua#L29-L34"
>
	Draws a rotating sprite which is defined by the `ProgressImageID` configuration settings.
</Method>