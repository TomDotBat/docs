import Method from "/components/method";
import Argument from "/components/method/argument";

# DrawNPCOverhead
<Method
	name="PIXEL.DrawNPCOverhead"
	realm="client"
	args={[
		<Argument type="Entity" name="npc">The NPC to draw over.</Argument>,
		<Argument type="string" name="text">The text to draw.</Argument>,
		<Argument type="Angle" name="angle" defaultValue="Angle(0, 0, 0)>Where the bubble should face relative to the NPC.</Argument>,
		<Argument type="Vector" name="position" defaultValue="npc:EyePos()>The relative position of the bubble.</Argument>,
		<Argument type="number" name="scale" defaultValue="0.05>The scale of the bubble.</Argument>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/drawing/cl_overheads.lua#L80-L94"
>
	Draws a text bubble above the given NPC.
</Method>