import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# GetTextSize
<Method
	name="PIXEL.GetTextSize"
	realm="client"
	args={[
		<Argument type="string" name="text">The text to get the size of.</Argument>,
		<Argument type="string" name="font">The identifier of the font.</Argument>
	]}
	returns={[
		<ReturnValue type="number" name="width">The width of the text.</ReturnValue>,
		<ReturnValue type="number" name="height">The height of the text.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/core/cl_fonts.lua#L82-L85"
>
	Calculates the size of the given text with the specified font.
</Method>