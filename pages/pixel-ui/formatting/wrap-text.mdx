import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# WrapText
<Method
	name="PIXEL.WrapText"
	realm="client"
	args={[
		<Argument type="string" name="text">The text to wrap.</Argument>,
		<Argument type="number" name="width">The width of the container to fit the text within.</Argument>,
		<Argument type="string" name="font">The font the text will be drawn with.</Argument>
	]}
	returns={[
		<ReturnValue type="string">The wrapped text.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/core/cl_text.lua#L131-L173"
>
	Wraps the given text to fit within the specified width.
</Method>