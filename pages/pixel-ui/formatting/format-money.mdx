import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# FormatMoney
<Method
	name="PIXEL.FormatMoney"
	realm="shared"
	args={[
		<Argument type="number" name="value">The number to convert.</Argument>
	]}
	returns={[
		<ReturnValue type="string">The value as a formatted money string.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/core/sh_formatting.lua#L40-L63"
>
	Converts the given value into a formatted money string according to the current gamemode settings.
</Method>