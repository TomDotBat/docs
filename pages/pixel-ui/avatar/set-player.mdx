import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# SetPlayer
<Method
	name="Avatar:SetPlayer"
	realm="shared-menu"
	args={[
		<Argument type="Player" name="ply">The player to use avatar of.</Argument>
		<Argument type="number" name="size">The size of the avatar to use. Acceptable sizes are 32, 64, 184.</Argument>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/elements/cl_avatar.lua#L43"
>
	Loads avatar for a given player.
</Method>