import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetRadius
<Method
	name="PIXEL.ColorPicker:GetRadius"
	realm="client"
	returns={[
		<ReturnValue type="number" name="radius">Picker radius.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the outer radius of the picker.
</Method>
