import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetRingThickness
<Method
	name="PIXEL.ColorPicker:GetRingThickness"
	realm="client"
	returns={[
		<ReturnValue type="number" name="thickness">Ring thickness.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the hue ring thickness.
</Method>
