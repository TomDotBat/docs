import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetTriangleRadius
<Method
	name="PIXEL.ColorPicker:GetTriangleRadius"
	realm="client"
	returns={[
		<ReturnValue type="number" name="radius">Triangle radius.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the triangle radius used for saturation/lightness selection.
</Method>
