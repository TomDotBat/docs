import Method from "/components/method";
import Argument from "/components/method/argument";

# OnValidate
<Method
	name="PIXEL.ValidatedTextEntry:OnValidate"
	realm="client"
	args={[
		<Argument type="boolean" name="valid">Whether the input is valid.</Argument>,
		<Argument type="string" name="message" defaultValue="nil">Validation feedback message.</Argument>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Callback fired after validation.
</Method>
