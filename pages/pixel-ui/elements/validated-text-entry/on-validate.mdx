# OnValidate

`PIXEL.ValidatedTextEntry:OnValidate(valid, message)`

Callback fired after validation.

**Parameters**

- `valid` (`boolean`) — Whether the input is valid.
- `message` (`string|nil`) — Validation feedback message.
