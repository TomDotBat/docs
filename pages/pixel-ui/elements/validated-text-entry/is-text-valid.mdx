import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# IsTextValid
<Method
	name="PIXEL.ValidatedTextEntry:IsTextValid"
	realm="client"
	args={[
		<Argument type="string" name="text">Input text to validate.</Argument>
	]}
	returns={[
		<ReturnValue type="boolean" name="valid">Whether the input is valid.</ReturnValue>,
		<ReturnValue type="string?" name="message">Validation feedback message.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Validates the current text value.
</Method>
