# IsTextValid

`PIXEL.ValidatedTextEntry:IsTextValid(text)`

Validates the current text value.

**Parameters**

- `text` (`string`) — Input text to validate.

**Returns**

- (`boolean`) `valid` — Whether the input is valid.
- (`string?`) `message` — Validation feedback message.
