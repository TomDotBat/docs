import Method from "/components/method";
import Argument from "/components/method/argument";

# SetPlayer

<Method
  name="Avatar:SetPlayer"
  realm="shared-menu"
  args={[
    <Argument type="Player" name="ply">
      The player to use avatar of.
    </Argument>,
    <Argument type="number" name="size">
      The size of the avatar, must be 32, 64 or 184.
    </Argument>,
  ]}
  repositoryUrl="https://github.com/TomDotBat/pixel-ui"
  declaration="lua/pixelui/elements/cl_avatar.lua#L43-L45"
>
  Loads avatar for a given player.
</Method>
