import Method from "/components/method";
import Argument from "/components/method/argument";

# SetSteamID
<Method
	name="PANEL:SetSteamID"
	realm="client"
	args={[
		<Argument type="string" name="id">SteamID64 or SteamID string.</Argument>,
		<Argument type="number" name="size" defaultValue="nil">Avatar image resolution hint.</Argument>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Sets the SteamID used by the avatar panel.
</Method>
