import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# AddCVar
<Method
	name="PIXEL.Menu:AddCVar"
	realm="client"
	args={[
		<Argument type="string" name="strText">Option label.</Argument>,
		<Argument type="string" name="convar">Console variable name.</Argument>,
		<Argument type="any" name="on">Value used for the enabled state.</Argument>,
		<Argument type="any" name="off">Value used for the disabled state.</Argument>,
		<Argument type="fun()" name="funcFunction" defaultValue="nil">Optional click callback.</Argument>
	]}
	returns={[
		<ReturnValue type="PIXEL.MenuOptionCVar" name="option">Created cvar option panel.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Creates and adds a cvar-bound option.
</Method>
