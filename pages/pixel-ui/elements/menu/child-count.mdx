import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# ChildCount
<Method
	name="PANEL:ChildCount"
	realm="client"
	returns={[
		<ReturnValue type="number" name="count">Child panel count.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the number of menu children.
</Method>
