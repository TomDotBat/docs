import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# GetChild
<Method
	name="PIXEL.Menu:GetChild"
	realm="client"
	args={[
		<Argument type="number" name="num">Child index.</Argument>
	]}
	returns={[
		<ReturnValue type="Panel|nil" name="child">Child panel.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns a child panel by 1-based index.
</Method>
