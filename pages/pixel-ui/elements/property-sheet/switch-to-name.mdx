import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# SwitchToName
<Method
	name="PANEL:SwitchToName"
	realm="client"
	args={[
		<Argument type="string" name="name">Tab name to activate.</Argument>
	]}
	returns={[
		<ReturnValue type="boolean" name="found">True when a tab was found and activated.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Switches to the first tab matching a name.
</Method>
