# SwitchToName

`PIXEL.PropertySheet:SwitchToName(name)`

Switches to the first tab matching a name.

**Parameters**

- `name` (`string`) — Tab name to activate.

**Returns**

- (`boolean`) `found` — True when a tab was found and activated.
