import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# AddSheet
<Method
	name="PIXEL.PropertySheet:AddSheet"
	realm="client"
	args={[
		<Argument type="string" name="label">Tab label.</Argument>,
		<Argument type="Panel" name="panel">Content panel.</Argument>,
		<Argument type="any" name="material" defaultValue="nil">Unused legacy icon argument.</Argument>,
		<Argument type="boolean" name="noStretchX" defaultValue="nil">When true, panel width is not stretched.</Argument>,
		<Argument type="boolean" name="noStretchY" defaultValue="nil">When true, panel height is not stretched.</Argument>,
		<Argument type="string" name="tooltip" defaultValue="nil">Tab tooltip text.</Argument>
	]}
	returns={[
		<ReturnValue type="table" name="sheet">Created sheet descriptor.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Adds a new tab and content panel to the property sheet.
</Method>
