import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetItems
<Method
	name="PIXEL.PropertySheet:GetItems"
	realm="client"
	returns={[
		<ReturnValue type="table" name="items">Registered sheet descriptors.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns all sheet entries.
</Method>
