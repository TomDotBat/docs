import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# SizeThink
<Method
	name="PIXEL.Frame:SizeThink"
	realm="client"
	args={[
		<Argument type="Panel" name="targetPanel">Target frame being resized.</Argument>,
		<Argument type="Panel" name="hoverPanel" defaultValue="nil">Optional hovered panel override.</Argument>
	]}
	returns={[
		<ReturnValue type="boolean|nil" name="isSizing">True when resize handling is active.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Handles resize cursor state and resizing movement.
</Method>
