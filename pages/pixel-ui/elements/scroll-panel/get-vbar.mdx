import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetVBar
<Method
	name="PIXEL.ScrollPanel:GetVBar"
	realm="client"
	returns={[
		<ReturnValue type="PIXEL.Scrollbar" name="vbar">Vertical scrollbar instance.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the vertical scrollbar panel.
</Method>
