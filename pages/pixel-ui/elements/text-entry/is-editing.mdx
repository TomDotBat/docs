import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# IsEditing
<Method
	name="PANEL:IsEditing"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="isEditing">True when focused for typing.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether this entry currently has keyboard focus.
</Method>
