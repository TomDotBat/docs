import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetTabbingDisabled
<Method
	name="PANEL:GetTabbingDisabled"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="disabled">True when tabbing is disabled.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether tabbing is disabled.
</Method>
