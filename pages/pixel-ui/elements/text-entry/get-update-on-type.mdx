import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetUpdateOnType
<Method
	name="PANEL:GetUpdateOnType"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="enabled">True when update-on-type is enabled.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether value updates fire while typing.
</Method>
