# AllowInput

`PIXEL.TextEntry:AllowInput(value)`

Callback used to filter raw character input.

**Parameters**

- `value` (`string`) — Input character.

**Returns**

- (`boolean|nil`) `blocked` — Return true to block this character.
