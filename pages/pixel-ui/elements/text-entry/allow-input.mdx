import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# AllowInput
<Method
	name="PIXEL.TextEntry:AllowInput"
	realm="client"
	args={[
		<Argument type="string" name="value">Input character.</Argument>
	]}
	returns={[
		<ReturnValue type="boolean" name="blocked">Return true to block this character.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Callback used to filter raw character input.
</Method>
