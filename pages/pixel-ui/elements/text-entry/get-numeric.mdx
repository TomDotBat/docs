import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetNumeric
<Method
	name="PIXEL.TextEntry:GetNumeric"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="enabled">True when numeric filtering is enabled.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether numeric-only input mode is enabled.
</Method>
