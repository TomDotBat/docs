import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetEnterAllowed
<Method
	name="PANEL:GetEnterAllowed"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="allow">True when Enter is allowed.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether Enter submits single-line input.
</Method>
