# GetAutoComplete

`PIXEL.TextEntry:GetAutoComplete(txt)`

Callback used to provide autocomplete suggestions.

**Parameters**

- `txt` (`string`) — Current text value.

**Returns**

- (`string[]|nil`) `suggestions` — Suggested completions.
