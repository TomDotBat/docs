import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetHistoryEnabled
<Method
	name="PANEL:GetHistoryEnabled"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="enabled">True when history is enabled.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether history navigation is enabled.
</Method>
