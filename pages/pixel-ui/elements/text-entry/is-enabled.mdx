import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# IsEnabled
<Method
	name="PANEL:IsEnabled"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="enabled">True when the entry accepts input.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether text input is enabled.
</Method>
