# ChooseOption

`PIXEL.ComboBox:ChooseOption(value, index)`

Selects a choice and updates displayed value.

**Parameters**

- `value` (`string`) — Selected display text.
- `index` (`number`) — Selected choice index.
