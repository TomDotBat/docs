import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# IsMenuOpen
<Method
	name="PANEL:IsMenuOpen"
	realm="client"
	returns={[
		<ReturnValue type="boolean" name="isOpen">True when menu panel is visible.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns whether the dropdown menu is currently open.
</Method>
