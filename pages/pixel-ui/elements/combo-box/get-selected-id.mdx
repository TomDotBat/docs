import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetSelectedID
<Method
	name="PANEL:GetSelectedID"
	realm="client"
	returns={[
		<ReturnValue type="number" name="index">Selected index.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the selected choice index.
</Method>
