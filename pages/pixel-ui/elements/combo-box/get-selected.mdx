import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetSelected
<Method
	name="PIXEL.ComboBox:GetSelected"
	realm="client"
	returns={[
		<ReturnValue type="string|nil" name="text">Selected display text.</ReturnValue>,
		<ReturnValue type="any" name="data">Selected payload.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
>
	Returns the currently selected text and data.
</Method>
