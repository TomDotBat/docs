# GetOptionText

`PIXEL.ComboBox:GetOptionText(id)`

Returns display text for a choice by index.

**Parameters**

- `id` (`number`) — Choice index.

**Returns**

- (`string|nil`) `text` — Choice label.
