import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# HexToColor
<Method
	name="PIXEL.HexToColor"
	realm="client"
	args={[
		<Argument type="string" name="hex">The hexadecimal color string.</Argument>
	]}
	returns={[
		<ReturnValue type="Color">A color which represents the hexadecimal string.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/core/cl_color.lua#L107-L114"
>
	Converts a hexadecimal string to a color.
</Method>