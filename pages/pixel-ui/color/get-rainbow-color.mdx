import { Callout } from "nextra/components";

import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# GetRainbowColor
<Method
	name="PIXEL.GetRainbowColor"
	realm="client"
	returns={[
		<ReturnValue type="Color">A color with the current values of the cycle effect.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/core/cl_color.lua#L124-L132"
>
	<Callout type="info" emoji="ℹ️">
		This method returns a new color instance every frame instead of modifying the values, if you wish to use the cycle effect you must call this before every draw.
	</Callout>
	Provides a colour which cycles through the rainbow.
</Method>