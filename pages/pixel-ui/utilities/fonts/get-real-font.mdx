import Method from "/components/method";
import Argument from "/components/method/argument";
import ReturnValue from "/components/method/return_value";

# GetRealFont
<Method
	name="PIXEL.GetRealFont"
	realm="client"
	args={[
		<Argument type="string" name="font">The identifier of the PIXEL font.</Argument>
	]}
	returns={[
		<ReturnValue type="string">The engine identifier of the PIXEL font.</ReturnValue>,
	]}
	repositoryUrl="https://github.com/TomDotBat/pixel-ui"
	declaration="lua/pixelui/core/cl_fonts.lua#L87-L89"
>
	Retrieves the engine identifier of a PIXEL font.
</Method>