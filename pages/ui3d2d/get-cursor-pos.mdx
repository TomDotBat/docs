import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# GetCursorPos
<Method
	name="ui3d2d.getCursorPos"
	realm="client"
	returns={[
		<ReturnValue type="number" name="x">The X position of the cursor.</ReturnValue>,
		<ReturnValue type="number" name="y">The Y position of the cursor.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/ui3d2d"
	declaration="cl_ui3d2d.lua#L131-L133"
>
	Returns the current position of the cursor.
	This will be nil when the player is not looking at the UI.
</Method>