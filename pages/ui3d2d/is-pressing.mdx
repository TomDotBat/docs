import Method from "/components/method";
import ReturnValue from "/components/method/return_value";

# IsPressing
<Method
	name="ui3d2d.isPressing"
	realm="client"
	returns={[
		<ReturnValue type="boolean">True when the user is pressing on the UI.</ReturnValue>
	]}
	repositoryUrl="https://github.com/TomDotBat/ui3d2d"
	declaration="cl_ui3d2d.lua#L52-L54"
>
	Returns true when the player holds down the use or attack key.
</Method>